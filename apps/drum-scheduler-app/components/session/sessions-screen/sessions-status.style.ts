import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  statusWrap: {
    paddingHorizontal: 16,
    paddingVertical: 8,
    flexDirection: 'row',
    alignItems: 'center',
    gap: 10,
  },
  statusText: {
    opacity: 0.7,
  },
});
