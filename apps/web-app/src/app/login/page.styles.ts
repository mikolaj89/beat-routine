import type { SxProps, Theme } from "@mui/material/styles";

export const LOGIN_BOX_WIDTH = 500;

export const styles: Record<string, SxProps<Theme>> = {
  page: {
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    minWidth: 500,
  },
};
